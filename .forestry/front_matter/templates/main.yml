---
label: Main
hide_body: true
fields:
- name: menu_text
  type: text
  config:
    required: false
  label: Menu button text
- name: first_name
  type: text
  config:
    required: false
  label: First name
- name: last_name
  type: text
  config:
    required: false
  label: Last name
- name: img_first
  type: file
  config:
    maxSize: 64
  label: Left image
- name: img_second
  type: file
  config:
    maxSize: 64
  label: Right image
pages:
- static/main.md
